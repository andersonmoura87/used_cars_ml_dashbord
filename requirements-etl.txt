# Dependências do pipeline ETL — instalar SEPARADO do requirements.txt
# Conflito: prefect 2.x usa starlette<0.33 enquanto fastapi>=0.109 usa starlette>=0.40
# Estes pacotes rodam em ambiente isolado (worker Prefect ou container separado)
#
# Instalar com: pip install -r requirements-etl.txt
# NÃO instalar junto com requirements.txt no mesmo venv

prefect==2.14.21
great-expectations==0.18.15

# Deps compartilhadas compatíveis com prefect 2.x
pandas>=1.5.0,<3.0
sqlalchemy>=1.4,<3.0
psycopg2-binary>=2.9
python-dotenv>=1.0.0
loguru>=0.7
